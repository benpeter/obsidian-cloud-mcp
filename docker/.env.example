# =============================================================================
# Obsidian Cloud MCP - Environment Configuration
# =============================================================================

# -----------------------------------------------------------------------------
# Domain (required for HTTPS)
# -----------------------------------------------------------------------------
# Your domain name pointing to this server
MCP_DOMAIN=mcp.yourdomain.com

# -----------------------------------------------------------------------------
# Cloudflare Worker (OAuth + Token Introspection)
# -----------------------------------------------------------------------------
# URL of your deployed Cloudflare Worker
CLOUDFLARE_WORKER_URL=https://your-worker.workers.dev

# Shared secret for token introspection endpoint auth (RFC 7662)
# Must match INTROSPECTION_SECRET set on the Cloudflare Worker
# Generate with: openssl rand -base64 32
INTROSPECTION_SECRET=your_introspection_secret_here

# -----------------------------------------------------------------------------
# Obsidian REST API
# -----------------------------------------------------------------------------
# Get this from Obsidian -> Settings -> Local REST API -> API Key
OBSIDIAN_API_KEY=your_api_key_here

# -----------------------------------------------------------------------------
# Web GUI Authentication
# -----------------------------------------------------------------------------
# Password for Web GUI (VNC) access
# Generate with: openssl rand -base64 16
VNC_PASSWORD=your_vnc_password_here
